---
const {tags} = Astro.props
---
<ul class="cloud-tags">
    {
        tags.map(tag => <li class=`tag-weight-${tag.weight}`><a class="tag-link" href=`/blog/tags/${tag.name.toLowerCase()}`>{tag.name}</a></li>)
    }
</ul>
